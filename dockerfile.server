FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY server/ server/
# Assuming the model is pulled from repo or previous step
COPY face_model.pkl . 
CMD ["uvicorn", "server.main:app", "--host", "0.0.0.0", "--port", "3000"]